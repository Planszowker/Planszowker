name: CMake Build with SFML

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: self-hosted
    container:
      image: ghcr.io/ishiguro96/ubuntu-planszowker:latest
      credentials:
        username: ishiguro96
        password: ${{ secrets.DOCKER_ACTIONS }}

    steps:
      - name: Create Build Environment
        shell: bash
        run: mkdir -p build

      - uses: actions/checkout@v2
      - name: Configure CMake
        working-directory: build
        shell: bash
        run: echo ${{github.workspace}}; ls -al ${{github.workspace}}; cmake ${{github.workspace}} -DCMAKE_BUILD_TYPE=Release

      - uses: actions/checkout@v2
      - name: Build
        working-directory: build
        shell: bash
        run: cmake --build . --config Release

      - uses: actions/checkout@v2
      - name: Test
        working-directory: build
        shell: bash
        run: ctest -C Release
